(ns duffel.ext.load
  "The purpose of this namespace is to load all the extension implementations,
  so that they will be parsed and added to the implementation set in core"
  (:require [duffel.ext.impl clerb ignore pacman put touch]))
